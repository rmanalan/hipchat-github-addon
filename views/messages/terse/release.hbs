<a href="{{release.author.html_url}}">{{release.author.login}}</a> {{action}} release "<a href="{{release.html_url}}">{{trim release.name length=30}}</a>" for <a href="{{repository.html_url}}">{{repository.full_name}}</a>