<a href="{{deployment_status.creator.html_url}}">{{deployment_status.creator.login}}</a> is deploying <a href="{{repository.html_url}}">{{repository.full_name}}</a> (revision {{deployment.sha}}){{#if deployment.environment}} to {{deployment.environment}}{{/if}}: {{deployment_status.description}} {{#if deployment_status.target_url}}<a href="{{deployment_status.target_url}}">#{{deployment_status.id}}</a>{{/if}}